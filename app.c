#include "app.h"

int main(int argc, char const *argv[])
{
    launchMenu();
    // FILE * f = fopen(LEADERBOARD_FILE, "w");
    // for (int i = 0; i < 10; i++)
    // {
    //     int score = 0;
    //     char * name = "test";
    //     fwrite(&score, sizeof(int), 1, f);
    //     fwrite(name, sizeof(char), 20, f);
    // }
    // fclose(f);
    return 0;
}
